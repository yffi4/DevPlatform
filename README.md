I did't use docker-compose.yml all services deploying on nomad by using job, also all infrastructure and statefull apps working local on host. I forgot to delete docker copose file. 
Last commit todat  it's this readme, no changes in the project.
